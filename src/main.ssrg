import * as http from "std/http/server"
import * as sqlite from "sqlite"
import { run } from "./server"

pub fn main
  -> Effect<{ httpServer: http.HttpServer, sqlite: sqlite.Sqlite }, String, Unit> =
  run ()
